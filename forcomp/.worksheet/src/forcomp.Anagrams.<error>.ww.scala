package forcomp

package Anagrams._

object ww {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(88); 
  val sentence = List("Linux", "rulez");System.out.println("""sentence  : List[String] = """ + $show(sentence ));$skip(110); val res$0 = 
 // combinations(sentenceOccurrences(sentence))

  dictionaryByOccurrences.get(List(('a',1),('t',1),('e',1)));System.out.println("""res0: <error> = """ + $show(res$0))}
                                
                                
   //subtract(List(),List())
                        
                        
}

package streams

object www {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(52); 
	val v = Vector(1,2,3);System.out.println("""v  : scala.collection.immutable.Vector[Int] = """ + $show(v ))}
}
